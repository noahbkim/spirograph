var count = 0;
var lengths = [];
var thetas = [];
var velocities = [];
var last = [];
var lines = [];

function reset() {
    lengths = new Array(count);
    thetas = new Array(count);
    velocities = new Array(count);
    last = new Array(count);
    for (var i = 0; i < count; i++)
        lengths[i] = thetas[i] = velocities[i] = 0;
}

function update() {
    var x, y;
    for (var i = 0; i < count; i++) {
        
        
    }
}

function draw() {
    
}

window.onload = function() {

    
    
}
